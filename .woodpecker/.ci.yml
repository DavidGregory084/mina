pipeline:
  ci:
    image: timbru31/java-node:${JAVA_VERSION}-jdk-${NODE_VERSION}
    when:
      event: [ push, pull_request, tag ]
    commands:
      - apt install -y xvfb
      - ./gradlew build --info

matrix:
  NODE_VERSION:
    - 16
  JAVA_VERSION:
    - 17